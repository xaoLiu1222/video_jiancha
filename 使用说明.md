# 视频审核系统 - 完整使用说明

## 📖 目录

1. [系统简介](#系统简介)
2. [环境准备](#环境准备)
3. [配置说明](#配置说明)
4. [使用方法](#使用方法)
5. [审核规则详解](#审核规则详解)
6. [常见问题](#常见问题)
7. [高级配置](#高级配置)

---

## 系统简介

本系统是一个基于**硅基流动 Qwen3-VL-72B 多模态大模型**的智能视频内容审核工具，专为音乐 MV 内容审核设计。

### 核心优势

- ✅ **完全免费** - 硅基流动提供 2000 万 tokens 免费额度，可审核约 10,000-20,000 个视频
- ✅ **准确率高** - 使用 72B 参数大模型，理解视频内容语义
- ✅ **自动分类** - 审核后自动将视频移动到对应目录
- ✅ **灵活配置** - 支持自定义视频路径（U 盘、外部硬盘等）
- ✅ **规则全面** - 支持 12 种审核规则，覆盖格式、内容、质量等多个维度

### 技术架构

```
视频输入 → 帧提取 → Base64 编码 → API 调用 → AI 分析 → 结果输出 → 自动分类
```

---

## 环境准备

### 系统要求

- **操作系统**: Linux / macOS / Windows
- **Python 版本**: 3.8 或更高
- **网络**: 需要稳定的互联网连接（调用云端 API）
- **磁盘空间**: 至少 500MB（用于存储审核结果）

### 安装步骤

#### 1. 克隆或下载项目

```bash
cd ~/桌面/myProject
git clone <项目地址>
cd video_jiancha
```

#### 2. 安装 Python 依赖

```bash
pip install -r requirements.txt
```

依赖包说明：
- `opencv-python` - 视频帧提取
- `requests` - API 调用
- `pyyaml` - 配置文件解析
- `pillow` - 图像处理

#### 3. 验证安装

```bash
python -c "import cv2, requests, yaml; print('依赖安装成功')"
```

---

## 配置说明

### 获取 API Key

1. 访问 [硅基流动官网](https://siliconflow.cn)
2. 注册并登录账号
3. 进入 **控制台** → **API 密钥**
4. 点击 **创建新密钥**，复制生成的 API Key（格式：`sk-xxxxxxxx`）

### 配置 API Key

有两种方式配置 API Key，**推荐使用配置文件方式**。

#### 方式一：配置文件（推荐）

编辑 `config/config.yaml` 文件：

```yaml
siliconflow:
  api_key: "sk-xxxxxxxx"  # 替换为你的 API Key
```

**优点**：
- 配置集中管理
- 支持版本控制（注意不要提交到公开仓库）
- 便于团队协作

#### 方式二：环境变量

```bash
# 临时设置（仅当前终端有效）
export SILICONFLOW_API_KEY='sk-xxxxxxxx'

# 永久设置（写入 ~/.bashrc）
echo 'export SILICONFLOW_API_KEY="sk-xxxxxxxx"' >> ~/.bashrc
source ~/.bashrc
```

**优先级**：环境变量 > 配置文件

### 配置文件详解

`config/config.yaml` 包含以下配置项：

```yaml
# ============ API 配置 ============
siliconflow:
  api_key: ""                              # API 密钥（必填）
  model: "Qwen/Qwen3-VL-72B-Instruct"     # 使用的模型
  api_base: "https://api.siliconflow.cn/v1"
  timeout: 180                             # API 超时时间（秒）

# ============ 目录配置 ============
paths:
  video_dir: "./data/to_review"            # 待审核视频目录
  approved_dir: "./data/approved"          # 通过视频存放目录
  rejected_dir: "./data/rejected"          # 拒绝视频存放目录
  manual_review_dir: "./data/manual_review" # 需人工复核目录
  results_path: "./data/results/review_results.json"

# ============ 视频处理配置 ============
video:
  num_frames: 6                            # 每个视频采样帧数
  min_duration: 120                        # 最小时长（秒）
  formats:                                 # 支持的视频格式
    - ".mp4"
    - ".avi"
    - ".mov"
    - ".ts"
    - ".mkv"
    - ".flv"

# ============ 图片压缩配置 ============
image:
  max_size: 512                            # 图片最大边长（像素）
  quality: 75                              # JPEG 压缩质量（1-100）

# ============ 审核规则配置 ============
review_rules:
  format_check_enabled: true               # 是否启用格式检查
  reject_vertical: true                    # 是否拒绝竖屏视频
  black_border_threshold: 20               # 黑边检测阈值（0-255）

# ============ 模型参数配置 ============
model_params:
  max_tokens: 512                          # 最大输出 token 数
  temperature: 0.3                         # 温度参数（0-1）
```

---

## 使用方法

### 基本使用

#### 1. 准备视频文件

将待审核的视频文件放入 `data/to_review/` 目录：

```bash
cp /path/to/your/videos/*.mp4 ./data/to_review/
```

#### 2. 运行审核

```bash
python siliconflow_review.py
```

#### 3. 查看结果

审核完成后，视频会自动分类到**输入目录下**的子文件夹：
- ✅ `<输入目录>/approved/` - 通过审核的视频
- ❌ `<输入目录>/rejected/` - 未通过审核的视频
- ❓ `<输入目录>/manual_review/` - 需要人工复核的视频

详细报告保存在 `<输入目录>/review_results.json`

**示例**：如果审核 `data/to_review/`，结果会在：
- `data/to_review/approved/`
- `data/to_review/rejected/`
- `data/to_review/manual_review/`
- `data/to_review/review_results.json`

### 高级使用

#### 指定视频目录

```bash
# 审核 U 盘中的视频
python siliconflow_review.py /media/username/USB_NAME/videos

# 审核桌面目录
python siliconflow_review.py ~/Desktop/mv

# 审核外部硬盘
python siliconflow_review.py /mnt/external_hdd/videos
```

#### 指定配置文件

```bash
python siliconflow_review.py -c /path/to/custom_config.yaml
```

#### 使用启动脚本

```bash
# Linux/macOS
./start_review.sh

# Windows
start_review.bat
```

### 代码调用示例

```python
from siliconflow_review import Config, SiliconFlowReviewer

# 加载配置
config = Config("./config/config.yaml")

# 初始化审核器
reviewer = SiliconFlowReviewer(
    config=config,
    video_dir="/media/usb/videos"
)

# 审核单个视频
result = reviewer.review_video("/path/to/video.mp4")
print(f"决策: {result['decision']}")
print(f"原因: {result['reason']}")
print(f"置信度: {result['confidence']}")

# 批量审核
video_paths = [
    "/path/to/video1.mp4",
    "/path/to/video2.mp4"
]
summary = reviewer.review_batch(video_paths)
print(f"总计: {summary['total']}")
print(f"通过: {summary['approved']}")
print(f"拒绝: {summary['rejected']}")
```

---

## 审核规则详解

系统支持 **12 种审核规则**，分为三大类：

### 格式类规则

| 规则 | 说明 | 检测方式 |
|------|------|----------|
| **竖屏视频** | 拒绝宽高比 < 1 的视频 | 本地检测 + AI 确认 |
| **上下黑边** | 有上下两条黑边直接拒绝 | AI 视觉识别 |
| **左右黑边** | 左右黑边超过屏幕一半拒绝 | AI 视觉识别 |
| **时长限制** | 视频时长少于 2 分钟 | 本地检测（不消耗 token）|

### 质量类规则

| 规则 | 说明 | 检测方式 |
|------|------|----------|
| **画面模糊** | 画质差、失焦、噪点严重 | AI 视觉识别 |
| **马赛克遮挡** | 演唱画面有马赛克遮挡 | AI 视觉识别 |

### 内容类规则

| 规则 | 说明 | 检测方式 |
|------|------|----------|
| **林夕作品** | 识别作词作曲为林夕的作品 | AI 语义理解 |
| **暴露/导向** | 内容暴露或导向有问题 | AI 内容理解 |
| **纯风景** | 只有风景画面无其他内容 | AI 场景识别 |
| **风景过长** | 风景画面超过视频一半时长 | AI 时序分析 |
| **广告内容** | 包含品牌 logo、联系方式等 | AI 视觉识别 |
| **吸毒画面** | 包含吸毒相关内容 | AI 内容理解 |

### 审核流程

```
1. 本地预检查（时长、格式）
   ↓
2. 视频帧提取（默认 6 帧）
   ↓
3. 图片压缩（节省 token）
   ↓
4. API 调用（AI 分析）
   ↓
5. 结果解析（JSON 格式）
   ↓
6. 自动分类（移动到输入目录下的子文件夹）
```

---

## 常见问题

### Q1: API Key 无效或过期

**错误信息**：`401 Unauthorized` 或 `Invalid API Key`

**解决方法**：
1. 检查 API Key 是否正确复制（注意空格）
2. 确认 API Key 未过期或被删除
3. 重新生成新的 API Key

### Q2: API 调用超时

**错误信息**：`Timeout` 或 `Connection timeout`

**解决方法**：
1. 检查网络连接是否稳定
2. 增加配置文件中的 `timeout` 值（默认 180 秒）
3. 减少 `num_frames` 值（减少数据传输量）

### Q3: 视频格式不支持

**错误信息**：`Unsupported video format`

**解决方法**：
在 `config/config.yaml` 中添加格式：

```yaml
video:
  formats:
    - ".mp4"
    - ".avi"
    - ".mov"
    - ".ts"
    - ".mkv"
    - ".flv"
    - ".webm"  # 添加新格式
```

### Q4: 如何审核 U 盘中的视频

**方法一：命令行指定**

```bash
# Linux/macOS
python siliconflow_review.py /media/username/USB_NAME/videos

# Windows
python siliconflow_review.py D:\videos
```

**方法二：修改配置文件**

```yaml
paths:
  video_dir: "/media/username/USB_NAME/videos"
```

### Q5: 免费额度用完了怎么办

**查看用量**：
1. 登录硅基流动控制台
2. 查看 **用量统计**

**超出后成本**：
- 约 0.001-0.002 元/视频
- 可充值继续使用

### Q6: 如何提高审核准确率

**方法**：
1. 增加 `num_frames` 值（更多帧采样）
2. 提高 `image.max_size` 值（更高分辨率）
3. 降低 `temperature` 值（更稳定输出）

**注意**：以上操作会增加 token 消耗

### Q7: 视频审核后找不到了

**原因**：视频已自动移动到分类目录

**查找位置**（在输入目录下）：
- `<输入目录>/approved/` - 通过的视频
- `<输入目录>/rejected/` - 拒绝的视频
- `<输入目录>/manual_review/` - 需复核的视频

**示例**：如果审核 `/media/usb/videos/`，视频会在：
- `/media/usb/videos/approved/`
- `/media/usb/videos/rejected/`
- `/media/usb/videos/manual_review/`

---

## 高级配置

### 性能优化

#### 减少 Token 消耗

```yaml
video:
  num_frames: 4  # 减少采样帧数（默认 6）

image:
  max_size: 384  # 降低图片分辨率（默认 512）
  quality: 60    # 降低压缩质量（默认 75）
```

#### 提高审核速度

```yaml
siliconflow:
  timeout: 120   # 减少超时时间（默认 180）

model_params:
  max_tokens: 256  # 减少输出长度（默认 512）
```

### 自定义审核规则

修改 `siliconflow_review.py` 中的 `review_prompt`：

```python
self.review_prompt = """审核音乐MV，返回JSON格式结果。

拒绝条件：
1.林夕作品 2.竖屏 3.上下黑边 4.左右黑边超一半
5.马赛克遮挡 6.画面模糊 7.暴露/导向问题
8.纯风景无人物 9.风景超一半时长 10.广告 11.吸毒画面
12.你的自定义规则  # 添加新规则

返回格式：{"decision":"approved或rejected","reason":"原因","confidence":0.95,"flags":[]}"""
```

### 批量处理脚本

创建 `batch_review.sh`：

```bash
#!/bin/bash

# 批量审核多个目录
DIRS=(
    "/media/usb1/videos"
    "/media/usb2/videos"
    "~/Desktop/mv"
)

for dir in "${DIRS[@]}"; do
    echo "正在审核: $dir"
    python siliconflow_review.py "$dir"
done

echo "所有目录审核完成"
```

### 定时任务

使用 cron 定时审核：

```bash
# 编辑 crontab
crontab -e

# 每天凌晨 2 点自动审核
0 2 * * * cd /home/nano/桌面/myProject/video_jiancha && python siliconflow_review.py
```

---

## 技术支持

- **项目地址**: [GitHub 仓库]
- **问题反馈**: [Issues]
- **硅基流动文档**: https://docs.siliconflow.cn
- **Qwen3-VL 文档**: https://github.com/QwenLM/Qwen-VL

---

## 更新日志

### v1.0.0 (2024-11-27)
- ✅ 支持 12 种审核规则
- ✅ 配置文件管理
- ✅ 自动分类功能
- ✅ 自定义视频目录
- ✅ 完整文档

---

**祝使用愉快！** 🎉
