# 项目文件清单

## 📁 项目结构

```
video_jiancha/
├── config/                          # 配置文件目录
│   ├── config.yaml                  # 实际配置文件（包含 API Key，不上传）
│   └── config.example.yaml          # 配置文件模板（上传到 GitHub）
│
├── data/                            # 数据目录
│   ├── to_review/                   # 待审核视频
│   ├── approved/                    # 通过的视频
│   ├── rejected/                    # 拒绝的视频
│   ├── manual_review/               # 需人工复核
│   ├── results/                     # 审核结果
│   └── features/                    # 特征数据
│
├── src/                             # 源代码目录
│   ├── features/                    # 特征提取模块
│   │   ├── __init__.py
│   │   └── video_encoder.py        # 视频编码器
│   ├── pipeline/                    # 审核流程模块
│   │   ├── __init__.py
│   │   ├── decision.py              # 决策逻辑
│   │   └── review_pipeline.py      # 审核管道
│   ├── utils/                       # 工具模块
│   │   ├── __init__.py
│   │   └── logger.py                # 日志工具
│   ├── whitelist/                   # 白名单模块
│   │   ├── __init__.py
│   │   ├── feature_store.py         # 特征存储
│   │   └── similarity.py            # 相似度计算
│   └── __init__.py
│
├── tests/                           # 测试目录
│
├── siliconflow_review.py            # 核心审核脚本 ⭐
├── start_review.sh                  # Linux/macOS 启动脚本
├── start_review.bat                 # Windows 启动脚本
├── requirements.txt                 # Python 依赖清单
│
├── README.md                        # 项目说明（英文，GitHub 首页）⭐
├── 使用说明.md                      # 完整使用指南（中文）⭐
├── 快速开始.md                      # 快速入门教程
├── Windows使用指南.md               # Windows 系统说明
├── GITHUB上传指南.md                # GitHub 上传流程 ⭐
├── 项目清单.md                      # 本文件
│
├── .gitignore                       # Git 忽略配置
├── LICENSE                          # MIT 许可证
│
├── venv/                            # Python 虚拟环境（不上传）
└── .spec-workflow/                  # 工作流配置（不上传）
```

## 📄 核心文件说明

### 1. 主程序文件

| 文件 | 说明 | 是否上传 |
|------|------|----------|
| `siliconflow_review.py` | 核心审核脚本，包含所有审核逻辑 | ✅ 是 |
| `start_review.sh` | Linux/macOS 快速启动脚本 | ✅ 是 |
| `start_review.bat` | Windows 快速启动脚本 | ✅ 是 |
| `requirements.txt` | Python 依赖包清单 | ✅ 是 |

### 2. 配置文件

| 文件 | 说明 | 是否上传 |
|------|------|----------|
| `config/config.yaml` | 实际配置文件（包含 API Key） | ❌ 否 |
| `config/config.example.yaml` | 配置文件模板（不含密钥） | ✅ 是 |

### 3. 文档文件

| 文件 | 说明 | 是否上传 |
|------|------|----------|
| `README.md` | 项目主说明文档（GitHub 首页） | ✅ 是 |
| `使用说明.md` | 完整使用指南（中文详细版） | ✅ 是 |
| `快速开始.md` | 快速入门教程 | ✅ 是 |
| `Windows使用指南.md` | Windows 系统使用说明 | ✅ 是 |
| `GITHUB上传指南.md` | GitHub 上传流程说明 | ✅ 是 |
| `项目清单.md` | 本文件，项目文件清单 | ✅ 是 |
| `LICENSE` | MIT 开源许可证 | ✅ 是 |

### 4. 源代码模块

| 目录/文件 | 说明 | 是否上传 |
|-----------|------|----------|
| `src/features/` | 特征提取模块 | ✅ 是 |
| `src/pipeline/` | 审核流程模块 | ✅ 是 |
| `src/utils/` | 工具函数模块 | ✅ 是 |
| `src/whitelist/` | 白名单功能模块 | ✅ 是 |

### 5. 数据目录

| 目录 | 说明 | 是否上传 |
|------|------|----------|
| `data/to_review/` | 待审核视频目录 | ❌ 否（仅上传 .gitkeep） |
| `data/approved/` | 通过视频目录 | ❌ 否（仅上传 .gitkeep） |
| `data/rejected/` | 拒绝视频目录 | ❌ 否（仅上传 .gitkeep） |
| `data/manual_review/` | 人工复核目录 | ❌ 否（仅上传 .gitkeep） |
| `data/results/` | 审核结果目录 | ❌ 否（仅上传 .gitkeep） |
| `data/features/` | 特征数据目录 | ❌ 否（仅上传 .gitkeep） |

### 6. 其他文件

| 文件/目录 | 说明 | 是否上传 |
|-----------|------|----------|
| `.gitignore` | Git 忽略配置 | ✅ 是 |
| `venv/` | Python 虚拟环境 | ❌ 否 |
| `__pycache__/` | Python 缓存文件 | ❌ 否 |
| `.spec-workflow/` | 工作流配置 | ❌ 否 |

## ⚠️ 上传前检查清单

在上传到 GitHub 前，请确认：

- [ ] `config/config.yaml` 中的 API Key 已清空或删除
- [ ] `config/config.example.yaml` 已创建（配置模板）
- [ ] `.gitignore` 配置正确
- [ ] 所有 `__pycache__` 目录已删除
- [ ] `data/` 目录下的视频文件不会被上传
- [ ] 所有文档已更新完善
- [ ] `README.md` 中的 GitHub 链接已更新为你的仓库地址

## 📊 文件统计

- **总文件数**: 约 30+ 个
- **代码文件**: 约 15 个 Python 文件
- **文档文件**: 6 个 Markdown 文件
- **配置文件**: 2 个 YAML 文件
- **脚本文件**: 2 个启动脚本

## 🎯 推荐上传顺序

1. **首次提交**: 所有核心文件和文档
2. **后续更新**: 根据修改内容分批提交

```bash
# 首次提交
git add .
git commit -m "Initial commit: 视频审核系统首次提交"
git push -u origin main

# 后续更新示例
git add siliconflow_review.py
git commit -m "feat: 优化审核逻辑"
git push
```

---

**准备完毕，可以开始上传了！** 🚀

请参考 [GITHUB上传指南.md](./GITHUB上传指南.md) 进行上传操作。
